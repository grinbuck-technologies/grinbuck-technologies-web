# Resend API key used by lib/resend.ts to send the pilot-kit and quote
# form submissions (see lib/actions/inquiries.ts). Get one from
# https://resend.com/api-keys. The sending domain (forms@grinbuck.com)
# must be verified in Resend for sends to succeed.
RESEND_API_KEY=
